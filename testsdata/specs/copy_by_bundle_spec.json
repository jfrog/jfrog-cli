{
    "files": [
      {
        "pattern": "${DIST_REPO1}/data/*",
        "target": "${DIST_REPO2}/",
        "flat": "false",
        "recursive": "true",
        "bundle": "${BUNDLE_NAME}/10"
      }
    ]
  }