name: Print Job Summary
on:
  push:
    branches:
      - '**'
jobs:
  insert-to-summary-file:
    runs-on: self-hosted
    steps:
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.20.x
          cache: false
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: check
        run: pwd && ls -la

      - name: Build current CLI
        run: go build .

      - name: Set log level
        run: export JFROG_CLI_LOG_LEVEL=DEBUG

      - name: Run CLI Ping
        run: ./jfrog-cli rt ping --url=${{ secrets.PLATFORM_URL }} --password=${{ secrets.PLATFORM_ADMIN_TOKEN }}

