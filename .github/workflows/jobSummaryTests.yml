name: Print Job Summary
on:
  push:
    branches:
      - '**'
jobs:
  JFrog-Job-Summary:
    strategy:
      matrix:
        os: [ ubuntu ]
    runs-on: ${{ matrix.os }}-latest
    steps:
      - uses: eyaldelarea/setup-jfrog-cli@fix-github-onprem
        env:
          JF_URL: ${{  secrets.JF_URL }}
          JF_ACCESS_TOKEN: ${{ secrets.JF_ACCESS_TOKEN }}
          JF_PROJECT: "jobsummary"

      # - name: Run dry run upload
      #   run: jf rt upload --spec=./testdata/filespecs/github_job_summaries.json

      - name: Run publish
        run: |
          jf --version
          jf rt bp firstBuild 123

      # - name: Run scan
      #   run: jf scan ./testdata/scanSummaryTests/fileOne.rpm

      # - name: Run scan
      #   run: jf build-scan firstBuild 123
      - name: add content
        run: |
          echo "<pre><a href="https://ecosysjfrog.jfrog.io/ui/packages?projectKey=jobsummary">📦 https jobsummary packages </a></pre>" >> $GITHUB_STEP_SUMMARY
          echo "<pre><a href="http://ecosysjfrog.jfrog.io/ui/packages?projectKey=jobsummary">📦 http jobsummary packages </a>"</pre> >> $GITHUB_STEP_SUMMARY
          echo "<details open>  <summary>  📦 Build info published to Artifactory by this workflow </summary><p></p>|  Build Info |  Time Stamp |  |---------|------------|  | [firstBuild 123](https://ecosysjfrog.jfrog.io/ui/builds/firstBuild/123/1720455410159/published?buildRepo=jobsummary-build-info&projectKey=jobsummary) | Jul 8, 2024 , 16:16:50 |       </details>  " >> $GITHUB_STEP_SUMMARY
          echo "<details open>  <summary>  📦 Build info published to Artifactory by this workflow </summary><p></p>|  Build Info |  Time Stamp |  |---------|------------|  | [firstBuild 123](http://ecosysjfrog.jfrog.io/ui/builds/firstBuild/123/1720455410159/published?buildRepo=jobsummary-build-info&projectKey=jobsummary) | Jul 8, 2024 , 16:16:50 |       </details>  " >> $GITHUB_STEP_SUMMARY
