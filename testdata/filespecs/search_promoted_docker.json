{
  "files": [
    {
      "pattern": "${DOCKER_PROMOTE_REPO}/${DOCKER_IMAGE_NAME}promotion*",
      "exclusions": ["${DOCKER_PROMOTE_REPO}/*sha256*","${DOCKER_PROMOTE_REPO}/repository.catalog"]
    }
  ]
}