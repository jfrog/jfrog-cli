{
	"auths": {
		"${CONTAINER_REGISTRY_DOMAIN_PUSH}": {
			"auth": "${RT_CREDENTIALS_BASIC_AUTH_PUSH}"
		},
		"${CONTAINER_REGISTRY_DOMAIN_PULL}": {
			"auth": "${RT_CREDENTIALS_BASIC_AUTH_PULL}"
		}
	}
}