{
  "files": [
    {
      "pattern": "ecosys-docker-local/(jfrog/jfrog-cli-${IDENTIFIER}/${VERSION}/*)",
      "target": "reg2/{1}"
    },
    {
      "pattern": "ecosys-docker-local/(jfrog/jfrog-cli-full-${IDENTIFIER}/${VERSION}/*)",
      "target": "reg2/{1}"
    }
  ]
}