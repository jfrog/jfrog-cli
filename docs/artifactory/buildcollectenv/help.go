package buildcollectenv

const Description = "Capture environment variables."

var Usage = []string{"jfrog rt bce <build name> <build number>"}

const Arguments string =
`	build name
		Build name.

	build number
		Build number.`