package deletereleasebundle

const Description = "Delete a release bundle."

var Usage = []string{"jfrog rt delrb [command options] <bundle name> <bundle version>"}

const Arguments string = `	bundle name
		Bundle name.

	bundle version
		Bundle version.`
