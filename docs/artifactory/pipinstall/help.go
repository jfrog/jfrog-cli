package pipinstall

const Description = "Run pip install."

var Usage = []string{`jfrog rt pipi [command options] <pip command>`}

const Arguments string = `	pip command
		Arguments and options for the pip command.`
