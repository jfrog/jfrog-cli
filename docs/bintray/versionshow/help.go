package versionshow

const Description string = "Show Version."

var Usage = []string{"jfrog bt vs [command options] <target version>"}

const Arguments string =
`	target version
		The argument specifies the path to the target version in Bintray.`