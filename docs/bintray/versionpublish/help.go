package versionpublish

const Description string = "Publish Version."

var Usage = []string{"jfrog bt vp [command options] <target version>"}

const Arguments string =
`	target version
		The argument specifies the path to the target version in Bintray.`