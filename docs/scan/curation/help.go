package curation

var Usage = []string{"curation [command options]"}

func GetDescription() string {
	return "curation your local project's dependencies by generating a dependency tree and check curation status ."
}
