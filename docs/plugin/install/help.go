package install

const Description = "Install plugin."

var Usage = []string{"jfrog plugin install <plugin name>"}

const Arguments string = `	plugin name
		Specifies the name of the JFrog CLI Plugin you wish to install from the plugins registry.`
